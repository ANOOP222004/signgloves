# Smart Glove Dataset Studio — Python Dependencies
# ============================================================
# Install all dependencies with:
#   pip install -r requirements.txt
#
# Then download the Vosk model separately (one-time, ~50MB):
#   wget https://alphacephei.com/vosk/models/vosk-model-small-en-us-0.15.zip
#   unzip vosk-model-small-en-us-0.15.zip -d voice/
#
# Works on Ubuntu and Windows — no compiler required.
# ============================================================

# --- Serial Communication (Phase 1) ---
pyserial>=3.5

# --- UI Framework (Phase 3) ---
PyQt5>=5.15

# --- Real-time plots (Phase 5) ---
pyqtgraph>=0.14

# --- 3D hand skeleton — OpenGL backend (Phase 6) ---
# pyqtgraph.opengl requires PyOpenGL; xcb_egl backend used on Ubuntu.
PyOpenGL>=3.1.0
PyOpenGL_accelerate>=3.1.0

# --- Numerical Processing ---
numpy>=1.24

# --- Voice Commands (Phase 4) ---
# sounddevice: cross-platform microphone access (works on Ubuntu + Windows)
# vosk: offline speech recognition — no internet required after model download
sounddevice>=0.4.6
vosk>=0.3.45
